$( '#multiple-select-clear-field' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );
$( '#multiple-select-clear-field2' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );
$( '#multiple-select-clear-field3' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );
$( '#multiple-select-clear-field4' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );
$( '#multiple-select-clear-field5' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );
$( '#multiple-select-clear-field6' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );
$( '#multiple-select-clear-field7' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );
$( '#multiple-select-clear-field8' ).select2( {
  closeOnSelect: true,
  allowClear: true,
} );

if(screen.width <= 991) {
  $('.journey-slick').slick({
    arrows: false
  });
}
